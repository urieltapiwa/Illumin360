# Support Portal (ASP.NET Core MVC, skeleton)

Server-rendered MVC frontend for **Support staff**. Talks ONLY to its own BFF
(`Illumin360.Support.Bff`) — never directly to domain microservices (charter Part 5/6).
Consumes design tokens exported from Figma as CSS variables (charter Part 17); targets WCAG 2.2 AA.

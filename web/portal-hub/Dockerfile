# Static landing hub for portal.illumin — links to the six MVC portals.
# Replaces the retired business-portal SPA + BFF.
FROM nginx:1.27-alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80

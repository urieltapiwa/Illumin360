# API Contracts (OpenAPI 3.1)
One spec per service, design-first. Generated specs are published here in CI (e.g. candidates.openapi.json
from `GET /openapi/v1.json`). Treat as the contract source of truth; never break a published contract.
